# Errandly
A hyperlocal gig economy task marketplace built with React + Flask.

## Stack
- **Frontend:** React + Vite + Tailwind CSS
- **Backend:** Flask + SQLAlchemy + SQLite + Razorpay

## Structure
- `/frontend` — React app (Vite)
- `/backend` — Flask REST API
